services:
  peopleops-agent:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./.runtime:/app/.runtime
      - ./.chroma:/app/.chroma
      - ./data:/app/data
